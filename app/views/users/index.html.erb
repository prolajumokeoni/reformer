<div class="row mt-5">
<table class="table table-striped">
    <thead>
      <th>Username</th>
      <th>Email</th>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to  user.username, user  %></td>
          <td><%=  user.email %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<a class="btn text-decoration-none btn-dark mt-5" href="/users/new">NEW USER</a>
